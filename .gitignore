# ============================================
# Dependencies
# ============================================
node_modules/
frontend/node_modules/
backend/node_modules/
# Commit package-lock.json for reproducible installs (npm ci in CI)
yarn.lock
.pnpm-store/
.yarn/cache
.yarn/unplugged
.pnp.*

# ============================================
# Environment & Secrets (NEVER commit these)
# ============================================
.env
.env.local
.env.production
.env.development
.env.test
.env.staging
.env.*.local
# Allow .env.example as template only
!.env.example

# Credentials, keys, certificates
*.pem
*.key
*.crt
*.cert
*.p12
*.pfx
*-credentials.json
*credentials*.json
service-account*.json
GOOGLE_APPLICATION_CREDENTIALS

# Support chat image uploads (local storage)
uploads/

# ============================================
# Logs & Debug
# ============================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ============================================
# OS & Editor
# ============================================
.DS_Store
.DS_Store?
._*
Thumbs.db
*.swp
*.swo
*~
*.bak
*.backup

# IDE
.vscode/
.idea/
.cursor/

# ============================================
# Build outputs (generated, not needed in repo)
# ============================================
# Ignore only root build output; frontend/dist is intentionally tracked for server deploys.
/dist/
!frontend/dist/
!frontend/dist/**
build/
# frontend/dist tracked so we can push built app for server (build locally, git push, pull on server)
*.map
*.min.js
*.min.css

# Docs folder (not needed for live) - only root README.md in live
docs/

# Cache
.cache/
.parcel-cache/
.vite/
.turbo/
*.tsbuildinfo

# ============================================
# Database (local dev only)
# ============================================
*.sqlite
*.sqlite3
*.db

# ============================================
# Testing
# ============================================
coverage/
.nyc_output/
playwright-report/
test-results/
e2e/test-results/
.last-run.json
**/trace.zip

# ============================================
# Shopify (local CLI state)
# ============================================
.shopify/

# ============================================
# Dev workflow (not needed for production deploy)
# ============================================
.github/
.husky/

# ============================================
# Deployment / hosting cache
# ============================================
.netlify/
.vercel/
.elasticbeanstalk/

# ============================================
# Misc
# ============================================
CURSOR_TROUBLESHOOTING.md
tmp/
temp/
*.tmp

