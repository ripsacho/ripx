# Worktree vs main project

Your repo has two working copies:

- **Main:** `/Users/m.a.k.ripon/Desktop/RipX` — open this in Cursor so edits go to "main".
- **Worktree:** `.cursor/worktrees/RipX/vlo` — used by Cursor for some operations.

Always **File → Open Folder →** your main path so your main files stay up to date.
